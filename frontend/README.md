# AniSense frontend

Templates in `templates/` and assets in `static/` are served by `api/main.py` using FastAPI and Jinja2. Run the application from the repository root with `python -m uvicorn api.main:app --reload`. See the [main README](../README.md) for dependencies and model setup.
